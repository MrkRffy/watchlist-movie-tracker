import 'package:watchlist/data/models/show.dart';

class Movie extends Show {
  Movie({
    poster,
    backdrop,
    title,
    id,
    year,
    numOfRatings,
    criticsReview,
    metascoreRating,
    rating,
    genre,
    plot,
    cast,
  });
}
